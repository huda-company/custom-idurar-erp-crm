name: Deploy to GCP

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # - name: Set up Node.js
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: 16

      # - name: cd into project dir
      #   run: cd /home/huda23aduh/custom-idurar-erp-crm

      # - name: pull origin develop
      #   run: git pull origin develop

      # - name: check git status
      #   run: git status

      # - name: Install dependencies
      #   run: npm install --only=prod

      # - name: Start application
      #   run: npm run start

      # - name: Set up Google Cloud SDK
      #   uses: google-github-actions/setup-gcloud@v0.3.0
      #   with:
      #     service_account_key: ${{ secrets.GCP_SA_KEY }}
      #     project_id: ${{ secrets.GCP_PROJECT_ID }}
      #     export_default_credentials: true

      # - name: Deploy to GCP
      #   run: |
      #     gcloud app deploy --version=${GITHUB_SHA::8}

      # - name: restart pm2
      #   run: pm2 restart server

      - name: deploy using ssh
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}  # GCP VM's IP or hostname
          username: ${{ secrets.USERNAME }}  # SSH username (e.g., ubuntu)
          key: ${{ secrets.SSH_PRIVATE_KEY }}  # Private key from secret
          run: |
            cd /home/huda23aduh/custom-idurar-erp-crm
            git fetch origin
            git pull origin develop  # Update code
            # git status
            npm install --only=prod
            pm2 restart server
